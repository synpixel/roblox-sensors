[package]
name = "synpixel/roblox-sensors"
description = ""
license = "MIT"
authors = ["synpixel"]
version = "0.1.0"

registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

exclude = ["**"]
include = ["src", "src/", "default.project.json", "wally.lock", "wally.toml"]

[dependencies]
